# API origin for the browser client.
# Leave unset in development — /api is proxied to the Express server.
# VITE_API_URL=https://your-api-host.com/api

# Override the dev proxy target if your API does not run on port 5000.
# VITE_PROXY_TARGET=http://localhost:5000
